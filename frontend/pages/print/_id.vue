<template>
  <h1>{{ id }}</h1>
</template>

<script lang="ts">
import Vue from 'vue';

export default Vue.extend({
  name: 'PrintPage',
  // async asyncData({ params }) {
  //   const id = await params.id
  //   return { id }
  // },
});
</script>
